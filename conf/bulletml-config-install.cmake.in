include("${CMAKE_CURRENT_LIST_DIR}/bulletml-config-targets.cmake")

if (WIN32)
    set(libdir "bin")
else ()
    set(libdir "lib@LIB_SUFFIX@")
endif ()

set(BULLETML_INCLUDE_DIR
    "@CMAKE_INSTALL_PREFIX@/include/d")
set(BULLETML_LIBRARY_DIR
    "@CMAKE_INSTALL_PREFIX@/${libdir}")
