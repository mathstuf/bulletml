module bulletml.elements._types;

public enum Motion {
  ABSOLUTE,
  RELATIVE,
  SEQUENCE
}

public enum Orientation {
  NONE,
  VERTICAL,
  HORIZONTAL
}

public enum Direction {
  AIM,
  ABSOLUTE,
  RELATIVE,
  SEQUENCE
}
